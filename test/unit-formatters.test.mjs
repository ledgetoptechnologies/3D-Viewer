import assert from 'node:assert/strict';import test from 'node:test';import {formatArea,formatElevation,formatLength,formatVolume,normalizeUnits} from '../unit-formatters.mjs';
test('units default to imperial and format all measurement dimensions',()=>{assert.equal(normalizeUnits(), 'imperial');assert.equal(formatLength(1),'3\' 3"');assert.equal(formatArea(4046.8564224),'1.00 acres');assert.equal(formatVolume(0.764554858),'≈ 1 cu yd');assert.equal(formatElevation(10),'33 ft');});
test('metric units format length area volume and elevation without imperial labels',()=>{assert.equal(formatLength(12.34,'metric'),'12.3 m');assert.equal(formatArea(125.5,'metric'),'125.5 m²');assert.equal(formatVolume(4.25,'metric'),'4.25 m³');assert.equal(formatElevation(10,'metric'),'10.0 m');});
