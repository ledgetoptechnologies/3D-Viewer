diff --git a/Obj2Tiles/Options.cs b/Obj2Tiles/Options.cs
index c2cd92b..98ab223 100644
--- a/Obj2Tiles/Options.cs
+++ b/Obj2Tiles/Options.cs
@@ -79,6 +79,12 @@ public class Options
     [Option("ktx2-uastc", Required = false, HelpText = "Use UASTC (higher quality, larger, transcodes to BC7/ASTC) instead of the default ETC1S/BasisLZ mode for KTX2 textures.", Default = false)]
     public bool Ktx2Uastc { get; set; }
 
+    [Option("max-parallelism", Required = false, HelpText = "Maximum concurrent tile conversions/writes. Resource-pressure failures retry once at one worker.", Default = 2)]
+    public int MaxParallelism { get; set; }
+
+    [Option("image-parallelism", Required = false, HelpText = "Maximum ImageSharp tasks inside each tile conversion.", Default = 1)]
+    public int ImageParallelism { get; set; }
+
     [Option("ktx-path", Required = false, HelpText = "Explicit path to the libktx native library (ktx.dll / libktx.so / libktx.dylib) or the directory containing it, used for --texture-format Ktx2. When omitted it is resolved from the OBJ2TILES_KTX environment variable, next to the executable (bundled), or on the system library path.", Default = null)]
     public string? KtxPath { get; set; }
 
diff --git a/Obj2Tiles/Program.cs b/Obj2Tiles/Program.cs
index 75dc38a..48efb23 100644
--- a/Obj2Tiles/Program.cs
+++ b/Obj2Tiles/Program.cs
@@ -48,6 +48,10 @@ namespace Obj2Tiles
                 return;
             }
 
+            // Obj2Tiles is a single-purpose process, so a process-wide ImageSharp ceiling prevents
+            // every outer tile worker from independently fanning out to all reported CPUs.
+            SixLabors.ImageSharp.Configuration.Default.MaxDegreeOfParallelism = opts.ImageParallelism;
+
             opts.Output = Path.GetFullPath(opts.Output);
             opts.Input = Path.GetFullPath(opts.Input);
 
@@ -122,7 +126,7 @@ namespace Obj2Tiles
 
                 var boundsMapper = await StagesFacade.Split(decimateRes.DestFiles, destFolderSplit, opts.Divisions,
                     opts.ZSplit, opts.KeepOriginalTextures, opts.SplitPointStrategy, opts.Octree, (float)opts.LodTextureScale,
-                    opts.MaxTextureSize, opts.TextureQuality, opts.TextureFormat);
+                    opts.MaxTextureSize, opts.TextureQuality, opts.TextureFormat, opts.MaxParallelism);
 
                 Console.WriteLine(" ?> Splitting stage done in {0}", sw.Elapsed);
 
@@ -175,7 +179,7 @@ namespace Obj2Tiles
                             : rootTextureSizeCap;
                         await StagesFacade.Split(rootSourceObj, rootTempDir, 0,
                             textureDownscale: rootDownscale, maxTextureSize: rootMaxTextureSize, textureQuality: opts.TextureQuality,
-                            textureFormat: opts.TextureFormat);
+                            textureFormat: opts.TextureFormat, maxParallelism: opts.MaxParallelism);
                         var compressedRoot = Directory.GetFiles(rootTempDir, "*.obj").FirstOrDefault();
                         if (compressedRoot != null)
                             rootSourceObj = compressedRoot;
@@ -209,7 +213,8 @@ namespace Obj2Tiles
                     };
                 }
 
-                StagesFacade.Tile(destFolderSplit, tilesetOutput, opts.LODs, baseError, boundsMapper, gpsCoords, opts.LocalMode, opts.Octree, rootSourceObj, gltfOptions);
+                StagesFacade.Tile(destFolderSplit, tilesetOutput, opts.LODs, baseError, boundsMapper, gpsCoords,
+                    opts.LocalMode, opts.Octree, rootSourceObj, gltfOptions, opts.MaxParallelism);
 
                 Console.WriteLine(" ?> Tiling stage done in {0}", sw.Elapsed);
 
@@ -227,6 +232,7 @@ namespace Obj2Tiles
                 // (e.g. a missing .mtl dependency) would look successful to batch/CI callers even
                 // though no output was produced.
                 Console.Error.WriteLine(" !> Exception: {0}", ex.Message);
+                RuntimeDiagnostics.Write("pipeline_failure", ex);
                 Environment.ExitCode = 1;
             }
             finally
@@ -331,6 +337,18 @@ namespace Obj2Tiles
                 return false;
             }
 
+            if (opts.MaxParallelism is < 1 or > 64)
+            {
+                Console.WriteLine(" !> --max-parallelism must be between 1 and 64");
+                return false;
+            }
+
+            if (opts.ImageParallelism is < 1 or > 64)
+            {
+                Console.WriteLine(" !> --image-parallelism must be between 1 and 64");
+                return false;
+            }
+
             return true;
         }
 
diff --git a/Obj2Tiles/RuntimeDiagnostics.cs b/Obj2Tiles/RuntimeDiagnostics.cs
new file mode 100644
index 0000000..9b9bc62
--- /dev/null
+++ b/Obj2Tiles/RuntimeDiagnostics.cs
@@ -0,0 +1,97 @@
+using System.Diagnostics;
+using System.Text.Json;
+
+namespace Obj2Tiles;
+
+internal static class RuntimeDiagnostics
+{
+    private const int MaxExceptionDepth = 12;
+
+    public static bool IsResourcePressure(Exception error)
+    {
+        foreach (var current in ExceptionChain(error))
+        {
+            if (current is TaskSchedulerException or OutOfMemoryException or InsufficientMemoryException)
+                return true;
+        }
+        return false;
+    }
+
+    public static void Write(string eventName, Exception? error = null, int? fromParallelism = null, int? toParallelism = null)
+    {
+        var process = Process.GetCurrentProcess();
+        ThreadPool.GetAvailableThreads(out var availableWorkers, out var availableIo);
+        ThreadPool.GetMaxThreads(out var maxWorkers, out var maxIo);
+        var gc = GC.GetGCMemoryInfo();
+        var payload = new
+        {
+            schemaVersion = 1,
+            eventName,
+            fromParallelism,
+            toParallelism,
+            resourcePressure = error != null && IsResourcePressure(error),
+            exceptionTypes = error == null ? Array.Empty<string>() : ExceptionChain(error).Select(item => item.GetType().FullName ?? item.GetType().Name).ToArray(),
+            exceptionHResults = error == null ? Array.Empty<int>() : ExceptionChain(error).Select(item => item.HResult).ToArray(),
+            resources = new
+            {
+                processorCount = Environment.ProcessorCount,
+                processThreads = Try(() => process.Threads.Count),
+                workingSetBytes = Try(() => process.WorkingSet64),
+                privateMemoryBytes = Try(() => process.PrivateMemorySize64),
+                gcHeapBytes = GC.GetTotalMemory(false),
+                gcTotalAvailableBytes = gc.TotalAvailableMemoryBytes,
+                gcHighMemoryThresholdBytes = gc.HighMemoryLoadThresholdBytes,
+                threadPoolAvailableWorkers = availableWorkers,
+                threadPoolMaxWorkers = maxWorkers,
+                threadPoolAvailableIo = availableIo,
+                threadPoolMaxIo = maxIo,
+                cgroupPidsCurrent = ReadMetric("/sys/fs/cgroup/pids.current"),
+                cgroupPidsMax = ReadMetric("/sys/fs/cgroup/pids.max"),
+                cgroupMemoryCurrent = ReadMetric("/sys/fs/cgroup/memory.current"),
+                cgroupMemoryMax = ReadMetric("/sys/fs/cgroup/memory.max")
+            }
+        };
+        Console.Error.WriteLine("OBJ2TILES_DIAGNOSTIC " + JsonSerializer.Serialize(payload));
+    }
+
+    private static IEnumerable<Exception> ExceptionChain(Exception error)
+    {
+        var pending = new Queue<Exception>();
+        var seen = new HashSet<Exception>(ReferenceEqualityComparer.Instance);
+        pending.Enqueue(error);
+        while (pending.Count > 0 && seen.Count < MaxExceptionDepth)
+        {
+            var current = pending.Dequeue();
+            if (!seen.Add(current)) continue;
+            yield return current;
+            if (current is AggregateException aggregate)
+            {
+                foreach (var inner in aggregate.Flatten().InnerExceptions) pending.Enqueue(inner);
+            }
+            else if (current.InnerException != null)
+            {
+                pending.Enqueue(current.InnerException);
+            }
+        }
+    }
+
+    private static long? Try(Func<long> read)
+    {
+        try { return read(); }
+        catch { return null; }
+    }
+
+    private static string? ReadMetric(string file)
+    {
+        try
+        {
+            var value = File.ReadAllText(file).Trim();
+            if (value.Length is < 1 or > 32) return null;
+            return value == "max" || value.All(char.IsAsciiDigit) ? value : null;
+        }
+        catch
+        {
+            return null;
+        }
+    }
+}
diff --git a/Obj2Tiles/Stages/SplitStage.cs b/Obj2Tiles/Stages/SplitStage.cs
index 6f70d87..285ad9f 100644
--- a/Obj2Tiles/Stages/SplitStage.cs
+++ b/Obj2Tiles/Stages/SplitStage.cs
@@ -10,7 +10,8 @@ public static partial class StagesFacade
     public static async Task<Dictionary<string, Box3>[]> Split(string[] sourceFiles, string destFolder, int divisions,
         bool zsplit, bool keepOriginalTextures = false, SplitPointStrategy splitPointStrategy = SplitPointStrategy.VertexBaricenter,
         bool isOctree = false, float lodTextureScale = 1.0f,
-        int maxTextureSize = 0, int textureQuality = 75, TextureFormat textureFormat = TextureFormat.Jpeg)
+        int maxTextureSize = 0, int textureQuality = 75, TextureFormat textureFormat = TextureFormat.Jpeg,
+        int maxParallelism = 2)
     {
         var results = new Dictionary<string, Box3>[sourceFiles.Length];
 
@@ -65,9 +66,8 @@ public static partial class StagesFacade
         Func<IMesh, Vertex3> replaySplitPoint = m =>
             splitPlan.TryGetValue(m.Name, out var pt) ? pt : baseSplitPoint(m);
 
-        // Split all LODs in parallel using the pre-computed split plan.
+        // Process LODs sequentially so their bounded tile writers cannot multiply concurrency.
         // In octree mode, the finest LOD (index=0) gets the most divisions; each coarser LOD gets one fewer.
-        var tasks = new List<Task<Dictionary<string, Box3>>>();
         for (var index = 0; index < sourceFiles.Length; index++)
         {
             var file = sourceFiles[index];
@@ -77,13 +77,9 @@ public static partial class StagesFacade
             int lodDivisions = isOctree ? divisions + sourceFiles.Length - index - 1 : divisions;
             float textureDownscale = index == 0 ? 1.0f : (float)Math.Pow(lodTextureScale, index);
 
-            tasks.Add(Split(file, dest, lodDivisions, zsplit, textureStrategy, splitPointStrategy, replaySplitPoint, textureDownscale, maxTextureSize, textureQuality, textureFormat));
+            results[index] = await Split(file, dest, lodDivisions, zsplit, textureStrategy, splitPointStrategy,
+                replaySplitPoint, textureDownscale, maxTextureSize, textureQuality, textureFormat, maxParallelism);
         }
 
-        await Task.WhenAll(tasks);
-
-        for (var i = 0; i < tasks.Count; i++)
-            results[i] = tasks[i].Result;
-
         return results;
     }
@@ -94,7 +90,8 @@ public static partial class StagesFacade
         TexturesStrategy textureStrategy = TexturesStrategy.Repack,
         SplitPointStrategy splitPointStrategy = SplitPointStrategy.VertexBaricenter,
         float textureDownscale = 1.0f,
-        int maxTextureSize = 0, int textureQuality = 75, TextureFormat textureFormat = TextureFormat.Jpeg)
+        int maxTextureSize = 0, int textureQuality = 75, TextureFormat textureFormat = TextureFormat.Jpeg,
+        int maxParallelism = 2)
     {
         Func<IMesh, Vertex3> getSplitPoint = splitPointStrategy switch
         {
@@ -104,7 +101,7 @@ public static partial class StagesFacade
             _ => throw new ArgumentOutOfRangeException(nameof(splitPointStrategy))
         };
 
-        return await Split(sourcePath, destPath, divisions, zSplit, textureStrategy, splitPointStrategy, getSplitPoint, textureDownscale, maxTextureSize, textureQuality, textureFormat);
+        return await Split(sourcePath, destPath, divisions, zSplit, textureStrategy, splitPointStrategy, getSplitPoint, textureDownscale, maxTextureSize, textureQuality, textureFormat, maxParallelism);
     }
 
     private static async Task<Dictionary<string, Box3>> Split(string sourcePath, string destPath, int divisions,
@@ -113,7 +110,8 @@ public static partial class StagesFacade
         SplitPointStrategy splitPointStrategy,
         Func<IMesh, Vertex3> getSplitPoint,
         float textureDownscale = 1.0f,
-        int maxTextureSize = 0, int textureQuality = 75, TextureFormat textureFormat = TextureFormat.Jpeg)
+        int maxTextureSize = 0, int textureQuality = 75, TextureFormat textureFormat = TextureFormat.Jpeg,
+        int maxParallelism = 2)
     {
         var sw = new Stopwatch();
         var tilesBounds = new Dictionary<string, Box3>();
@@ -185,7 +183,11 @@ public static partial class StagesFacade
         var progress = 0;
         var lodName = Path.GetFileName(destPath);
 
-        Parallel.ForEach(ms, m =>
+        Parallel.ForEach(ms, new ParallelOptions
+        {
+            MaxDegreeOfParallelism = maxParallelism,
+            TaskScheduler = TaskScheduler.Default
+        }, m =>
         {
             var n = Interlocked.Increment(ref progress);
             m.DebugName = $"{lodName}-Mesh-{n}/{ms.Length}";
diff --git a/Obj2Tiles/Stages/TilingStage.cs b/Obj2Tiles/Stages/TilingStage.cs
index 284870d..2136d4e 100644
--- a/Obj2Tiles/Stages/TilingStage.cs
+++ b/Obj2Tiles/Stages/TilingStage.cs
@@ -11,11 +11,11 @@ public static partial class StagesFacade
 {
     public static void Tile(string sourcePath, string destPath, int lods, double baseError, Dictionary<string, Box3>[] boundsMapper,
-        GpsCoords? coords = null, bool localMode = false, bool isOctree = false, string? rootSourceObj = null, GltfConverterOptions? gltfOptions = null)
+        GpsCoords? coords = null, bool localMode = false, bool isOctree = false, string? rootSourceObj = null, GltfConverterOptions? gltfOptions = null, int maxParallelism = 2)
     {
 
         Console.WriteLine(" ?> Working on objs conversion");
 
-        ConvertAllB3dm(sourcePath, destPath, lods, gltfOptions);
+        ConvertAllB3dm(sourcePath, destPath, lods, gltfOptions, maxParallelism);
 
         // Give the tileset root renderable content. The root tile spans the whole model but, in an
         // octree/multi-tile layout, its geometry lives only in the child tiles, leaving the root
@@ -215,7 +215,7 @@ public static partial class StagesFacade
 
     }
 
-    private static void ConvertAllB3dm(string sourcePath, string destPath, int lods, GltfConverterOptions? gltfOptions = null)
+    private static void ConvertAllB3dm(string sourcePath, string destPath, int lods, GltfConverterOptions? gltfOptions, int maxParallelism)
     {
         var filesToConvert = new List<Tuple<string, string>>();
 
@@ -233,11 +233,16 @@ public static partial class StagesFacade
             }
         }
 
-        Parallel.ForEach(filesToConvert, (file) =>
+        Console.WriteLine($" ?> B3DM conversion workers: {maxParallelism}; files: {filesToConvert.Count}");
+        Parallel.ForEach(filesToConvert, new ParallelOptions
+        {
+            MaxDegreeOfParallelism = maxParallelism,
+            TaskScheduler = TaskScheduler.Default
+        }, file =>
         {
             Console.WriteLine($" -> Converting to b3dm '{file.Item1}'");
             Utils.ConvertB3dm(file.Item1, file.Item2, gltfOptions);
         });
     }
 
     // Duomo of Milan
